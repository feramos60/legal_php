	
		<!-- Navbar -->
		<nav class="main-header navbar navbar-expand navbar-white navbar-light">
			<!-- Left navbar links -->
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" data-widget="pushmenu" href="#" role="button">
						<i class="fas fa-bars"></i>
					</a>
				</li>
				<li class="nav-item d-none d-sm-inline-block">
					<a href="{{ ruta }}/dashboard/index" class="nav-link">Inicio</a>
				</li>
				
			</ul>

			<!-- Right navbar links -->
			<ul class="navbar-nav ml-auto">
				

				<!-- Messages Dropdown Menu -->

				<!-- Notifications Dropdown Menu -->



				{# User Dropdown Menu #}
				<li class="dropdown user user-menu">
					<!-- Menu Toggle Button -->
					<a href="#" class="btn dropdown-toggle" data-toggle="dropdown">
						<!-- The user image in the navbar-->
						<img {% if user.userimg %} src="/liga/App/user/{{ user.userimg }}" {% else %} src="{{ ruta }}/dist/img/white.png" {% endif %} class="user-image" alt="User Image Menu">
						<!-- hidden-xs hides the username on small devices so only the image appears. -->
						<span class="hidden-xs">{{ user.first_name }}</span>
					</a>
					<ul class="dropdown-menu">
						<!-- The user image in the menu -->
						<li class="user-header">
							<img {% if user.userimg %} src="/liga/App/user/{{ user.userimg }}" {% else %} src="{{ ruta }}/dist/img/white.png" {% endif %} class="img-circle" alt="User Image">

							<p>
								{{ user.first_name }}
								- {% if user.role_id == 1 %}SuperAdmin{% elseif user.role_id == 2 %}Admin {% else %} Usuario{% endif %}
								<small>Miembro desde {{ user.created_at|date("d M Y") }}</small>
							</p>
						</li>
						<!-- Menu Body -->
						<li class="user-body">
							<div class="row"></div>
							<!-- /.row -->
						</li>
						<!-- Menu Footer-->
						<li class="user-footer">
							<div class="float-md-left">
								<a href="{{ ruta }}/profile/show" class="btn btn-default ">Perfil</a>
							</div>
							<div class="float-md-right">
								<a href="{{ ruta }}/logout" class="btn btn-default">Salir</a>
							</div>
						</li>
					</ul>
				</li>
				{#
				<li class="nav-item">
					<a class="nav-link" data-widget="fullscreen" href="#" role="button">
						<i class="fas fa-expand-arrows-alt"></i>
					</a>
				</li>
				quitada la opción de pantalla completa #}
			</ul>
		</nav>
		<!-- /.navbar -->
